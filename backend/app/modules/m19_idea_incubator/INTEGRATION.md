# Module 19 integration
Mount `spec.router`. Replace the in-memory run map with tenant-scoped PostgreSQL idea runs, append-only events, optimistic expected-stage checks, idempotency keys and queue priority. Inject public market search/Product Hunt API, sandboxed code and screenshot artifacts. Generated code runs with network/resource limits, dependency/license/security scans, deterministic test logs, bounded repair attempts and a kill switch. Preview deployment stays behind Module 0 approval; production deploy is deliberately absent. Add a PDF renderer through Module 15. Acceptance: budget cap, approval gate, append-only evidence provenance, no fabricated preview URL, cross-tenant isolation, duplicate job protection and retry stop conditions.
