# Module 14 integration

Replace the in-process project repository with the tenant-scoped SQL adapter at integration. Dispatch only approved plans through Celery. Register five specialist runners (literature, data, coder, analyst, writer) behind the same budget, timeout, provenance, and audit contracts. Generated code runs only in an approved, no-network sandbox. `Service.commit` supports GitPython versioning inside a project-specific working tree and never pushes a remote. Advancement pass: validated DAGs, measurable gates, bounded refinement budgets, artifact hashes/provenance, resumable task states and fail-closed approval proposals.

## 2026-09-20 rows 510-534 (m14 lane)

`engineering.py` adds typed engineering design artifacts for features-doc rows 510-534: 25 registered design kinds (requirements, architecture, API, schema, monolith, microservices, event-driven, message-queue, caching, load-balancing, auto-scaling, fault-tolerance, DR, backup, security, authn, authz, encryption, key-management, audit-logging, compliance, privacy, governance, lineage, data-quality), each row-mapped with required sections. Deterministic scaffold generation (LLM refinement can follow); validation enforces sections, rejects placeholders, rejects operational-state claims (design is never deployment evidence), and rejects embedded secrets (named-KMS references pass). Documents register fail-closed through the existing artifact pipeline as kind `design_document` with generator/design_kind/feature_row provenance. Routes: POST /projects/{id}/designs (generate + validate + register, 201), GET /projects/{id}/designs. Designs flow into exports via the standard artifact payload path. No new shared dependencies.
