# Feature evidence: rows 771-809

Typed mounted API: `POST /v1/modules/17/trust-wellbeing/coaching`. All 39 rows map to
unique typed skills and focused, low-risk reflection guidance. Every response includes
non-diagnosis, no-inferred-identity/emotion, review-only, no-external-action, and explicit
crisis/medical escalation text. Parametrized tests cover every row and separately require
stop/adapt/professional-support language for wellbeing rows 797-809, non-compulsory
forgiveness/tolerance, student authorship, and authenticated owner isolation.

Declared gaps: no diagnosis, treatment, medical or fitness plan, crisis assessment,
suicide-risk classification, inferred emotion/identity/condition, therapy, biometric
analysis, sleep tracking, exercise prescription, or outcome promise. This cannot replace
emergency services, crisis resources, clinicians, disability-aware professional advice,
or human organizational accountability. Trust, safety, inclusion, justice, and wellbeing
are not scored or inferred. Suggested practices are optional and must stop if unsafe or
distressing.

## State machine upgrade (rows 710-809)

Each row now also dispatches to a named callable `row_<id>_<method>` in
`coaching_state_machines_710_809.py` running a method-specific state machine with
required-input validation, distinctive computed analysis, and evaluation/uncertainty
envelopes. Manipulative intent is refused; consent-gated (mediation, voting, role
playing) and authorization-gated (red teaming) methods block without proof; wellbeing
rows carry a crisis-escalation gate. Mounted at `POST /v1/modules/17/coaching/state-machine`
(owner-scoped, 422 on unknown method or missing inputs). Per-row distinctive tests and
failure paths live in `tests/modules/test_m17_coaching_state_machines_710_809.py`.
